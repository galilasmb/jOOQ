
sudo: required
language: java

jdk:
  - openjdk8

script:
  - mvn -DskipTests=true package

before_deploy:
    - mkdir MiningBuild
    - find . -name '*.jar' -exec cp {} ./MiningBuild \;
    - cd /home/travis/build/galilasmb/jOOQ/MiningBuild
    - tar -zcvf result.tar.gz *
deploy:
  provider: releases
  api_key: $GITHUB_TOKEN
  file: result.tar.gz
  name: fetchjar-cc04a7fd812f7f2f2e016fc22c2cb9581ac039f9
  file_glob: true
  overwrite: false
  skip_cleanup: true
  on:
    all_branches: true 
            